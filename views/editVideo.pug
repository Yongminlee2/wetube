extends layouts/main

block content
    .form-container
        form(action=routes.editVideo, method="post")
            input(type="text", placeholder="Title", name="title", value="TEXT")
            textarea(name="description", placeholder="Description")=TEST
            input(type="submit", value="Update Video")
        a.form-container__link(href=routes.deleteVideo)
            button.delete Delete Video